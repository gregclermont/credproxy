mitmproxy>=10.0
aiohttp>=3.9
pyyaml>=6.0
# Sandboxed Starlark runtime for scripted injection schemes (design-v3 phase 3).
# Prebuilt manylinux cp312 wheels -> no Rust toolchain in the image.
# >=2025.2.5 for OpaquePythonObject; FrozenModule.call kwargs landed 2025.2.3.
starlark-pyo3>=2025.2.5
pytest>=7.0
pytest-aiohttp>=1.0
